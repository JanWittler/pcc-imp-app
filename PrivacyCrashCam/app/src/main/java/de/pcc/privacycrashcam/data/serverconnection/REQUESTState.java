package de.pcc.privacycrashcam.data.serverconnection;

/**
 * Default state enum to determine if a server request was successful.
 *
 * @author Giorgio Gross
 */
public enum RequestState {
    SUCCESS,
    FAILURE;
}
